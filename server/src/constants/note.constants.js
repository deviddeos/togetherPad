export const NOTE_VISIBILITY = Object.freeze({
  PUBLIC: "public",
  PROTECTED: "protected",
});

export const NOTE_STATES = Object.freeze({
  PUBLIC: "public",
  PASSWORD_REQUIRED: "password_required",
  NOT_FOUND: "not_found",
  OPENED: "opened",
});
